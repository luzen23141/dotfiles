#! /bin/zsh

if [ "$#" -gt 0 ]; then
  # 先暫存第一個變數
  subCommand=$1

  # 移除第一個變數
  shift 1

  command_path="a-${subCommand}"

  if command -v "$command_path" >/dev/null 2>&1; then
    "$command_path" "$@"
  else
    echo "找不到指令: $command_path"
    exit 1
  fi
else
  echo "a backup"
fi
