#! /bin/zsh

source "$DOTFILES"/helperFunc/configHelper

config_value="$(getAConfig go_run_default "")"

if [ -n "$config_value" ]; then
  cmd_parts=("${(@z)config_value}")
  "${cmd_parts[@]}" "$@"
else
  go run . "$@"
fi
